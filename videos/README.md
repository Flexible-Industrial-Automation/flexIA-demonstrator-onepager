# Drop your clips here

The page auto-detects a video the moment you put a file at the right path — no HTML editing needed. If a file is missing, the panel just shows the placeholder as it does now.

| Workflow stage | File to add |
|---|---|
| 01 · Model — flexModeler | `videos/01-flexmodeler-architecture-view.mp4` |
| 02 · Implement — flexCommunicator | `videos/02-flexcommunicator-component-build.mp4` |
| 03 · Verify — consistency & correctness | `videos/03-consistency-check-arch-vs-hw.mp4` |
| 04 · Deploy — flexCloud + flexEngine | `videos/04-flexcloud-fleet-deployment.mp4` |
| Monitor — flexCloud | `videos/05-flexcloud-centralized-monitoring.mp4` |
| Connect — flexCommunicator | `videos/06-flexcommunicator-it-ot-bridge.mp4` |
| Reason — flexCloud AI agent | `videos/07-flexcloud-ai-reasoning-agent.mp4` |

**Format:** `.mp4` (H.264), roughly 16:10, no audio required (clips are muted/looped by default — narration works too, viewers can unmute via the native controls once a clip is playing).

**Usage:** just save the file with the exact name above into this folder and reload the page. Click the panel once a clip has loaded to play/pause it.
